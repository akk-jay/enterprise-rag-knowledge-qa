# 用户提示词模板（User Prompt）

将此内容复制到 Dify 工作流 LLM 节点的"用户提示词"输入框中。

---

用户问题：{{#sys.query#}}

参考文档内容如下：

{{#context#}}

请根据以上文档内容回答用户问题。如果文档中没有相关信息，请明确告知。
```

> **说明：** `{{#sys.query#}}` 是 Dify 内置变量，代表用户输入的问题。`{{#context#}}` 是知识检索节点返回的文档片段。
